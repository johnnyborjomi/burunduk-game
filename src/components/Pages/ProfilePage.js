import { Link } from 'react-router-dom';

const ProfilePage = () => {
    return (
        <div className="setting-content">
            <h4>Profile</h4>
        </div>
    );
};

export default ProfilePage;
