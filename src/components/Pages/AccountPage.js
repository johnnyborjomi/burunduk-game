import { Link } from 'react-router-dom';

const AccountPage = () => {
    return (
        <div className="setting-content">
            <h4>Account</h4>
        </div>
    );
};

export default AccountPage;
